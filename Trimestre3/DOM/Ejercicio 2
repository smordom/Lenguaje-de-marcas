1.Muestra el título de los CD

title = "";
n = document.getElementsByTagName("TITLE").length;
for(i = 0; i < n; i++)
  title += document.getElementsByTagName("TITLE")[i].childNodes[0].nodeValue + " <br> ";

2.Muestra el precio

price = "";
n = document.getElementsByTagName("PRICE").length;
for(i = 0; i < n; i++)
  price += document.getElementsByTagName("PRICE")[i].childNodes[0].nodeValue + " <br> ";

3.Muestra la compañía

company = "";
n = document.getElementsByTagName("COMPANY").length;
for(i = 0; i < n; i++)
  company += document.getElementsByTagName("COMPANY")[i].childNodes[0].nodeValue + " <br> ";
